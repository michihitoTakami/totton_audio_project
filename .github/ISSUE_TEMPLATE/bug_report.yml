name: "🐛 バグ報告"
description: "不具合の報告（再現手順・期待結果・実結果を記載）"
title: "[Bug] "
labels:
  - bug
body:
  - type: markdown
    attributes:
      value: |
        ありがとうございます。再現性のある情報があるほど調査が速くなります。

  - type: textarea
    id: summary
    attributes:
      label: 概要
      description: 何が起きているかを簡潔に。
      placeholder: 例) Web UIからEQを有効化するとエラーになる
    validations:
      required: true

  - type: textarea
    id: steps
    attributes:
      label: 再現手順
      description: 手順を箇条書きで。
      placeholder: |
        1. ...
        2. ...
        3. ...
    validations:
      required: true

  - type: textarea
    id: expected
    attributes:
      label: 期待する動作
      placeholder: 例) EQが有効になり、ステータスが更新される
    validations:
      required: true

  - type: textarea
    id: actual
    attributes:
      label: 実際の動作
      placeholder: 例) 500エラーが返り、EQが有効にならない
    validations:
      required: true

  - type: dropdown
    id: environment
    attributes:
      label: 実行環境
      description: 該当するものを選択してください。
      options:
        - PC (開発)
        - Jetson (本番)
        - Raspberry Pi (関連コンポーネント)
        - その他
    validations:
      required: true

  - type: textarea
    id: versions
    attributes:
      label: バージョン/コミット
      description: 該当するタグやコミットSHAがあれば。
      placeholder: 例) v0.1.0 / 1a2b3c4

  - type: textarea
    id: logs
    attributes:
      label: ログ/スクリーンショット
      description: 可能な範囲で貼り付けてください（機密情報はマスク）。
      render: shell
